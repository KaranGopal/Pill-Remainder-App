<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.pill">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".getStarted">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Welcome" />
        <activity android:name=".Today" />
        <activity android:name=".Theropy" />
        <activity android:name=".Settings" />
        <activity android:name=".Report" />
        <activity android:name=".Regular" />
        <activity android:name=".Record" />
        <activity android:name=".Planned" />
        <activity android:name=".MyAlarm" />
        <activity android:name=".Medication" />
        <activity android:name=".Login" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".AddYourReminder" />
        <activity android:name=".Activity" />
        <activity android:name=".AboutUs" />
        <activity android:name=".SignUp" />
    </application>

</manifest>